# frozen_string_literal: true

RSpec.describe ShipCompliantV1Rb do
  it "has a version number" do
    expect(ShipCompliantV1Rb::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
